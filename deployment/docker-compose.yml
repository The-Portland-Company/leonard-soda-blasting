version: '3.8'

services:
  frontend:
    build:
      context: ../
      dockerfile: deployment/Dockerfile.frontend
    ports:
      - "3574:3574"
    environment:
      - REACT_APP_DIRECTUS_URL=http://backend:8574
    depends_on:
      - backend

  backend:
    build:
      context: ../
      dockerfile: deployment/Dockerfile.backend
    ports:
      - "8574:8574"
    environment:
      - NODE_ENV=production
      - PORT=8574
    env_file:
      - ../backend/.env
    depends_on:
      - postgres

  postgres:
    image: postgres:15
    environment:
      - POSTGRES_DB=directus
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: