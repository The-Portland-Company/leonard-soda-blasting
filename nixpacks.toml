[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cp -r frontend/. . && npm install --legacy-peer-deps --force"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s build -p $PORT"

[variables]
NODE_ENV = "production"